apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-maven-settings
  namespace: "{{ nexus_output_namespace }}"
data:
  settings.xml: |- 
    {{ lookup('file', nexus_mvn_repository_path) | indent(4) }}
