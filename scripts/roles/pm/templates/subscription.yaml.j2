apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: processmining-subscription
  namespace: "{{ pm_project_name }}"
spec:
  channel: "{{ pm_operator_channel }}"
  # TODO manual approval installPlanApproval: Manual
  name: ibm-automation-processmining
  source: ibm-automation-processminings
  sourceNamespace: openshift-marketplace
  # TODO manual approval startingCSV: "{{ pm_starting_csv }}"
