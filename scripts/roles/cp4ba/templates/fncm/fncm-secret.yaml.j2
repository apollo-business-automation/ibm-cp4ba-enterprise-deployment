apiVersion: v1
kind: Secret
metadata:
  name: ibm-fncm-secret
  namespace: "{{ cp4ba_project_name }}"
type: Opaque
stringData:
  gcdDBUsername: gcd
  gcdDBPassword: "{{ cp4ba_universal_password }}"
  os1DBUsername: os1
  os1DBPassword: "{{ cp4ba_universal_password }}"
  appLoginUsername: cpadmin
  appLoginPassword: "{{ cp4ba_universal_password }}"
  keystorePassword: "{{ cp4ba_universal_password }}"
  ltpaPassword: "{{ cp4ba_universal_password }}"
