kind: Secret
apiVersion: v1
metadata:
  name: external-tls-secret
  namespace: "{{ cp4ba_project_name }}"
data:
  cert.key: "{{ _tls_key }}"
  cert.crt: "{{ _tls_crt }}"
  ca.crt: "{{ _ca_tls_crt }}"
type: Opaque
