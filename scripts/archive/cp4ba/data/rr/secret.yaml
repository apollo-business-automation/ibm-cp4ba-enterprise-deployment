apiVersion: v1
kind: Secret
metadata:
  name: resource-registry-admin-secret
type: Opaque
stringData:
  rootPassword: '{{UNIVERSAL_PASSWORD}}'
  readUser: reader
  readPassword: '{{UNIVERSAL_PASSWORD}}'
  writeUser: writer
  writePassword: '{{UNIVERSAL_PASSWORD}}'
